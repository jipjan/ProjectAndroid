package com.experience.essteling.a3.esstelingexperience;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.TextView;

public class Lijst_spec extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_lijst_spec);

        Attractie attractie = (Attractie) getIntent().getSerializableExtra("PROJECT");

        TextView textView = (TextView)findViewById(R.id.)
    }
}
